import { RightContainer } from '../../styles/styledComponent'

function RightLayer({ type, data }) {
   return (
      <RightContainer>
         <h4>Home, right Layer</h4>
      </RightContainer>
   )
}

export default RightLayer
